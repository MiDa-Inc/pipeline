command: env | grep '^HERDR' | sort
exit: 0
session: pipeline_spike (isolated headless server)
note: NOT a herdr command. Auxiliary capture: run INSIDE isolated pane w1:p6 via `herdr pane run`,
note: recording the HERDR_* variables that pane's shell actually sees. Raw stdout, unmodified.
